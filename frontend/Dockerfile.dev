# 프론트엔드 개발용 Dockerfile
FROM node:18-alpine

# 작업 디렉토리 설정
WORKDIR /app

# package.json과 package-lock.json 복사
COPY package*.json ./

# 의존성 설치
RUN npm install

# 포트 노출 (Vite 개발 서버)
EXPOSE 5173

# 환경 변수 설정
ENV NODE_ENV=development

# Vite 개발 서버 실행 (소스 코드는 볼륨으로 마운트됨)
# --host 옵션으로 외부 접근 허용
CMD ["npm", "run", "dev", "--", "--host"]
